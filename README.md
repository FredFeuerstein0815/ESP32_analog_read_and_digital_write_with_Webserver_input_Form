Mit diesem Script liest ein ESP32 analoge Werte und schaltet in Abhängigkeit dieser Werte GPIOs an oder aus.
Die vorgegebenen Werte lassen sich auf dem Webserver einstellen. (Bis jetzt nur ein Wert(, vom delay abhängig) alle 20 Sekunden).
